#proj
project(resource-pipeline-core)

file(GLOB_RECURSE ALL_FILES RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} 
    ${CMAKE_CURRENT_LIST_DIR}/include/*.[ch]pp
    ${CMAKE_CURRENT_LIST_DIR}/include/*.h
)

file(GLOB_RECURSE TEST_FILES RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} 
    ${CMAKE_CURRENT_LIST_DIR}/test/*.[ch]pp
    ${CMAKE_CURRENT_LIST_DIR}/test/*.h
)

add_library(${PROJECT_NAME} INTERFACE ${ALL_FILES})

target_include_directories(${PROJECT_NAME} INTERFACE "${CMAKE_CURRENT_SOURCE_DIR}/include")

target_link_libraries(${PROJECT_NAME} INTERFACE yaml-cpp::yaml-cpp)

target_compile_options(${PROJECT_NAME} INTERFACE
  $<$<CXX_COMPILER_ID:MSVC>:/W4>
  $<$<NOT:$<CXX_COMPILER_ID:MSVC>>:-Wall -Wextra -Wpedantic>
)