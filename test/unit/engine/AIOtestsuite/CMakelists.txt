project(engine_aio_unit_test)

file(GLOB_RECURSE ALL_FILES RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} 
    ${CMAKE_CURRENT_LIST_DIR}/*.[ch]pp
    ${CMAKE_CURRENT_LIST_DIR}/*.h
)

set(ALL_LIBS
  PRIVATE GTest::gtest
  PRIVATE engine
  PRIVATE engine_ecs_unit_test
  PRIVATE engine_reflection_unit_test
  PRIVATE engine_serialisation_unit_test
)

add_executable(${PROJECT_NAME} ${ALL_FILES})

add_test(NAME engine_aio_test_plan COMMAND ${PROJECT_NAME})

target_link_libraries(${PROJECT_NAME} ${ALL_LIBS})

GROUP_UNIT_TEST_TARGETS(${PROJECT_NAME})